RTWord {
  input = item+
  item =
    | space -- space
    | html -- html
    | separator -- separator
    | string -- string
    | word -- word

  word = legalchar+
  legalchar = ~separator ~space any

  html = "<" "/"? letter+ ">"
  separator = ~alnum any
  comment = "//" (~"\n" any)* "\n"
  space += comment
  string = dq (~dq any)* dq
  dq = "\""

}

Word <: RTWord {
//  separator += ";" | ":" | "{" | "}" | "->" | ","
}
